var hooks = function () {

  this.Before(function (scenario) {
    // Will get called before a scenario
  });

  this.After(function (scenario) {
    // Will get called after a scenario
  });
};

module.exports = hooks;
